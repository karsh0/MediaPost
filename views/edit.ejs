<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <h5 class="mb-5">Edit your post.</h5>
        <form action="/edit/<%= post._id %>" method="post">
            <textarea name="content" placeholder="What's on your mind ? " class="w-1/3 p-2 resize-none bg-transparent border-2 border-zinc-800 rounded-md"><%= post.content %></textarea>
            <input class="px-3 py-2 bg-yellow-500 block rounded-md mt-2 w-40" type="submit" value="Update">
        </form>
    </div>
</body>
</html>
